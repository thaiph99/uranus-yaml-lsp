{
  "name": "uranus-yaml-workspace",
  "version": "1.0.4",
  "description": "Argo Workflow YAML navigation for VS Code and Language Server Protocol clients",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "watch": "npm run watch --workspaces",
    "test": "npm run build:core && npm run build:lsp && npm run test -w packages/core && npm run test -w packages/lsp-server",
    "build:core": "npm run build -w packages/core",
    "build:lsp": "npm run build -w packages/lsp-server",
    "build:vscode": "npm run build -w packages/vscode-extension",
    "install:all": "npm install && npm install --workspaces",
    "package:vscode": "npm run package -w packages/vscode-extension",
    "package:lsp": "npm pack -w packages/lsp-server"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
